spring:
  application:
    name: CITIZENSERVICE
  cloud:
    config:
      uri: http://localhost:8888
      name: citizenservice
      fail-fast: true
      retry:
        initial-interval: 1000
        max-interval: 2000
        max-attempts: 6
        multiplier: 1.1

