spring:
  application:
    name: API-GATEWAY
  cloud:
    config:
      uri: http://localhost:8888
      name: api-gateway
      fail-fast: true
      retry:
        initial-interval: 1000
        max-interval: 2000
        max-attempts: 6
        multiplier: 1.1

