spring:
  application:
    name: NOTIFICATIONSERVICE
  cloud:
    config:
      uri: http://localhost:8888
      name: notificationservice
      fail-fast: true
      retry:
        initial-interval: 1000
        max-interval: 2000
        max-attempts: 6
        multiplier: 1.1

